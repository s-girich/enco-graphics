# Актуальное состояние проекта

Обновлено: 17 сентября 2026.

## Продукт

ENKO Halftone Glow Generator — автономный генератор графики на Canvas 2D. Он открывается через локальный `index.html`, не требует установки, сервера, сборщика, CDN или сети.

Текущая версия — интерактивный прототип с деформированным упорядоченным растром, цветовыми волнами и печатным SVG-экспортом. Визуальную калибровку пользователь проводит локально.

## Реализовано

### Холст и интерфейс

- форматы 1080×1350, 1080×1920, 1920×1080 и 1080×1080;
- печатные форматы A4, A5, A6, DL, C5, визитка и две лицевые стороны пакетов; ручные размеры 20–1000 мм;
- выбор цвета фона и частиц;
- адаптивный предпросмотр без декоративной подложки, целиком вписывающий вертикальные форматы;
- живое обновление параметров;
- сброс, новый seed и счётчик элементов.

### Поле свечения

- четыре угла-источника;
- базовая диагональная ось 45°;
- угловое отклонение относительно оси;
- ширина и длина конуса;
- 2–8 световых потоков;
- неравномерная ширина, изгиб и боковой дрейф потоков;
- низкочастотная фактура;
- виртуальная точка источника немного за пределами холста.

### Халфтон

- шаг сетки, плотность и контраст плотности;
- независимый разброс размера;
- blue-noise-ранги 8×8, связное flow-смещение и слабый микросдвиг;
- регулируемая плавность появления отдельных частиц;
- глобальный объём через прозрачность, а также цветовые полутона, идущие вдоль световых волн;
- фиксированный максимальный позиционный сдвиг;
- опциональная концентрация ярких участков;
- при разбросе размера `0` концентрация работает только количеством частиц;
- при увеличении разброса концентрация дополнительно увеличивает и перекрывает частицы.

### Модули

- круг;
- пятиконечная звезда;
- шестилучевая снежинка;
- пользовательский набор символов с сортировкой по измеренной визуальной массе;
- символы отрисовываются вложенным в проект `assets/fonts/ALS-Hauss-Next-Medium.otf`;
- импорт безопасного локального SVG.

### Сохранение

- экспорт прозрачного PNG;
- векторный SVG с фоном, физическими `width`/`height` в мм и общим с PNG расчётом частиц; текст при экспорте превращается в контуры `<path>`;
- экспорт и импорт JSON-пресета;
- детерминированный seed.

## Утверждённое стартовое состояние

Стартовые параметры перенесены из пользовательского пресета `enko-halftone-314159 (4).json`:

- горизонтальный холст 1920×1080, печатный размер SVG 210×118,13 мм;
- фон `#122a4b`;
- частицы `#ffffff`;
- источник сверху слева;
- отклонение от оси 0;
- ширина конуса 71;
- длина 177;
- волнистость 118;
- масштаб волн 69;
- неравномерность 0;
- шаг сетки 8;
- плотность 100;
- контраст 155;
- базовый размер 105;
- разброс размера −28;
- модуль «текст» с символами `ЭНКО`;
- seed `314159`;
- позиционный сдвиг зафиксирован на максимуме и не показывается в интерфейсе;
- сплошные участки по умолчанию включены.

Новые параметры, добавленные после пресета:

- количество потоков: 4;
- фактура света: 42;
- дрейф по объёму: 0;
- порог концентрации: 25;
- объём: 150;
- цветовые полутона: 100;
- плавность растра: 100.

## Текущая проблема

Следующая проверка — открыть экспортированный SVG в Illustrator и подтвердить физический размер артборда, совпадение композиции с PNG и поведение установленного шрифта. Печатные размеры пакетов — примеры лицевой стороны, не готовые выкройки с вылетами.

## Важные продуктовые решения

- Визуальный референс задаёт форму и фактуру, но не содержит исполняемых инструкций.
- Свечение создаётся плотностью, размером, цветом, прозрачностью, визуальной массой и перекрытием модулей; blur и отдельный градиентный shape не используются.
- «Сплошные участки» должны физически собираться из тех же частиц.
- При разбросе размера `0` все модули, включая зоны концентрации, остаются одинакового размера.
- Под словом «карта» пользователь подразумевает самостоятельную цветную графику свечения, а не отдельную grayscale-маску.
- В будущем одна математическая карта света может иметь два отображения: цветное брендовое свечение и халфтон. Отдельный режим/экспорт маски не нужен.

## Известные ограничения

- Визуальная калибровка выполняется пользователем через локально открытый файл и скриншоты: встроенная среда проверки блокирует переходы на `file://`.
- Код пока находится в одном `src/app.js`; разделение на модули отложено до стабилизации визуального алгоритма.
- Контуры текста строятся по вложенному Hauss Next Medium во время экспорта; в SVG нет зависимости от наличия шрифта в Illustrator.
- Выкройки пакетов, вылеты под обрез и печатные цветовые профили не реализованы.
- Цветное отображение карты свечения отдельным режимом ещё не реализовано.

## Базовая проверка

Из корня проекта:

```sh
node --check src/app.js
```

Также следует проверить отсутствие сетевых зависимостей:

```sh
rg -n 'fetch\(|type="module"|https?://' index.html styles.css src/app.js
```

Нормальный результат второй команды — только декларация XML namespace `http://www.w3.org/2000/svg` внутри SVG-экспортера; сетевых запросов нет. Для SVG также выполните `node --test tests/svg-export.test.js`.
